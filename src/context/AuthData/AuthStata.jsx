import React,{useState, useContext} from 'react'
import AuthContext from './AuthContext'

const AuthStata = () => {
    const [authUser, setauthUser] = useState()
    const [isLoggedin, setisLoggedin] = useState(false)
  return (
    <div>
        
    </div>
  )
}

export default AuthStata